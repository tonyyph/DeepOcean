# Marketing Screenshot Skill

Rules:
- Keep existing marketing templates unless asked to redesign
- Only replace iPhone screen content with real screenshots from /screenshots
- Never invent UI
- Never recreate app screens manually
- Never blur, stretch, crop incorrectly, or cover important content
- Every screen inside a device frame must come from a real screenshot file

For each exported marketing image, create:
marketing-image-name.json

Include:
{
  "screenshot_sources": []
}
